{
  "name": "nobleport-deploy-bot",
  "version": "1.0.0",
  "description": "Website deployment bot with step-by-step progress indicators",
  "main": "deploy.js",
  "bin": {
    "deploy-bot": "./deploy.js"
  },
  "scripts": {
    "deploy": "node deploy.js",
    "deploy:preview": "node deploy.js --preview",
    "deploy:production": "node deploy.js --production"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
